#include <stdio.h>
#incldue <stdlib.h>

typedef struct Avistamento
{
    char H[5]; // horario
    char N[100]; // nome
} Avistamento;

int main(void)
{
    // Variaveis
    int P = 0, Q = 0, M = 0;
    Avistamento avistamentos[] = NULL;

    // Leitura de entradas
    scanf("%d %d", &P, &Q);

    return 0;
}